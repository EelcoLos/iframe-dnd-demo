name: PR Preview Deployment

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: write
  pull-requests: write

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    if: github.event.action != 'closed'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages

      - name: Prepare and deploy preview files
        run: |
          # Create preview directory
          mkdir -p gh-pages/pr-preview/pr-${{ github.event.number }}
          
          # Copy public files directly
          cp public/* gh-pages/pr-preview/pr-${{ github.event.number }}/
          cp index.html gh-pages/pr-preview/pr-${{ github.event.number }}/
          
          # Ensure .nojekyll exists at root
          touch gh-pages/.nojekyll
          
          # Configure git and commit
          cd gh-pages
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Deploy preview for PR #${{ github.event.number }}" || echo "No changes to commit"
          git push origin gh-pages

      - name: Comment PR with preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.issue.number;
            // Use custom domain if configured
            const previewUrl = `http://gh.eelcolos.nl/iframe-dnd-demo/pr-preview/pr-${prNumber}/`;
            
            const comment = `## 🎯 PR Preview Deployed!
            
            Your changes have been deployed to a preview environment:
            
            🔗 **Preview URL:** ${previewUrl}
            
            The preview will be automatically updated when you push new commits to this PR.
            
            ---
            <sub>Preview deployed from commit ${{ github.sha }}</sub>`;
            
            // Find existing preview comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });
            
            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('PR Preview Deployed')
            );
            
            if (botComment) {
              // Update existing comment
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: comment
              });
            } else {
              // Create new comment
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: comment
              });
            }

  cleanup-preview:
    runs-on: ubuntu-latest
    if: github.event.action == 'closed'
    steps:
      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Remove preview directory
        run: |
          rm -rf pr-preview/pr-${{ github.event.number }}
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Remove preview for PR #${{ github.event.number }}" || echo "No changes to commit"
          git push origin gh-pages || echo "Nothing to push"
